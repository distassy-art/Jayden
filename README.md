# Jayden

`bdcegypt-public-html/` is an Apache overlay for **bdcegypt.com** only: real `index.html` for `/queue-system/`, `/nurse-call-system/`, and `/self-service-kiosks/`, plus hide `__next` indexes. Home branding, About/Contact, and www are unchanged. No qserveai.com redirects.

See `bdcegypt-public-html/UPLOAD.md`.