{
  "name": "farsai-calendar",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types --include-runtime false",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.131.1"
  }
}
